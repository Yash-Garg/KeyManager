<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/authFragment">

    <fragment
        android:id="@+id/authFragment"
        android:name="dev.yash.keymanager.ui.auth.AuthFragment"
        android:label="@string/app_name"
        tools:layout="@layout/auth_fragment">
        <action
            android:id="@+id/action_authFragment_to_sshFragment"
            app:destination="@id/homeFragment" />
    </fragment>

    <fragment
        android:id="@+id/homeFragment"
        android:name="dev.yash.keymanager.ui.home.HomeFragment"
        android:label="@string/app_name"
        tools:layout="@layout/home_fragment">
        <action
            android:id="@+id/action_homeFragment_to_navigation"
            app:destination="@id/navigation" />
    </fragment>
    <navigation
        android:id="@+id/navigation"
        app:startDestination="@id/sshFragment">
        <fragment
            android:id="@+id/sshFragment"
            android:name="dev.yash.keymanager.ui.ssh.SshFragment"
            android:label="ssh_fragment"
            tools:layout="@layout/ssh_fragment" />
        <fragment
            android:id="@+id/gpgFragment"
            android:name="dev.yash.keymanager.ui.gpg.GpgFragment"
            android:label="gpg_fragment"
            tools:layout="@layout/gpg_fragment">
            <action
                android:id="@+id/action_gpgFragment_to_gpgDetailsFragment"
                app:destination="@id/gpgDetailsFragment" />
        </fragment>
        <fragment
            android:id="@+id/gpgDetailsFragment"
            android:name="dev.yash.keymanager.ui.details.GpgDetailsFragment"
            android:label="gpg_details_fragment"
            tools:layout="@layout/gpg_details_fragment" />
    </navigation>
</navigation>
