<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/authFragment">

    <fragment
        android:id="@+id/authFragment"
        android:name="dev.yash.keymanager.ui.AuthFragment"
        android:label="@string/app_name"
        tools:layout="@layout/auth_fragment">
        <action
            android:id="@+id/action_authFragment_to_sshFragment"
            app:destination="@id/sshFragment" />
    </fragment>

    <fragment
        android:id="@+id/sshFragment"
        android:name="dev.yash.keymanager.ui.SshFragment"
        android:label="@string/app_name"
        tools:layout="@layout/ssh_fragment" />
</navigation>
